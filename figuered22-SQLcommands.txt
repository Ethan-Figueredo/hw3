1. CREATE DATABASE CHEESECAKE;

2. USE CHEESECAKE;

3. CREATE TABLE ORDERS(
    ORDERID INT NOT NULL,
    MONTH CHAR(3) NOT NULL,
    DAY INT NOT NULL,
    QUANTITY INT NOT NULL,
    TOPPING CHAR(9) NOT NULL,
    NOTES CHAR(512) NOT NULL);

4. DESC ORDERS;

6. SELECT * FROM ORDERS;

7. SELECT * FROM ORDERS WHERE TOPPING = 'Plain';

8. INSERT INTO ORDERS (ORDERID, MONTH, DAY, QUANTITY, TOPPING, NOTES)
VALUES ('62', 'SEP', '1', '2', 'Cherry', 'Step 8');
